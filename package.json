{
  "name": "tarmarapi",
  "version": "1.0.50",
  "description": "An API for the classic Tarot de Marseille buil with Bun!",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "seed": "bun --bun run src/functions/seed.ts",
    "migrate": "bun --bun run src/db/migrate.ts"
  },
  "dependencies": {
    "@libsql/client": "^0.6.0",
    "drizzle-orm": "^0.30.6",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "drizzle-kit": "^0.20.14"
  },
  "module": "src/index.js"
}
